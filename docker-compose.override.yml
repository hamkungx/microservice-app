version: '3.4'

services:
  catalogdb:
     container_name: catalogdb
     restart: always
     ports:
        - "27018:27018"
     volumes:
        - catalogdb_data:/data/db
     networks:
        - micro-app-network
